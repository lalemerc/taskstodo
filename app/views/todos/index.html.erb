<h1>Todos#index</h1>

<ul><% @todos.each do |todo| %>
  <li>
 <%= link_to todo.name, todo_path(todo) %>


<%= link_to "Delete", todo_path(todo),
      method: :delete,
      data: { confirm: "Are you sure?" } %></li>

      <% end %></ul>

      <p><%= link_to "create new task", new_todo_path %></p>